\ProvidesPackage{custom-packages/tw-scrbook} [2019/07/10 v0.01 scrbook template enhancements]
\NeedsTeXFormat{LaTeX2e}
%\usepackage[parfill]{parskip}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{babel}
\usepackage{amsmath}
\usepackage{listings}
% To avoid warning:
% "Class scrbook Warning: \float@addtolists detected!"
%\usepackage{scrhack}

%//1.5 cm Zeilenabstand mit folgender Zeile
\usepackage[onehalfspacing]{setspace}

\usepackage{xcolor}
\usepackage{lmodern}
\usepackage[autostyle=true,english=american]{csquotes}

\usepackage[hyphens]{url}
%\usepackage[a4paper]{geometry}
\usepackage{layouts}

% Import custom listing styles
\input{custom-packages/lststyles}

\usepackage[
    citestyle=authoryear,
    bibstyle=authortitle,
    backend=biber,
    maxcitenames=1,
    maxbibnames=99,
    sorting=nty,
    date=comp,
    seconds=true,
    urldate=iso,
    dashed=false,
    uniquename=false,
    uniquelist=false,
    autocite=inline,%% behavior of autocite
    isbn=false,
    doi=false,
    url=false
]{biblatex}
\addbibresource{Sample.bib}

% Set these two counters to 3 so that in scrbook subsubsections are numbered and
% indexed in toc
\setcounter{tocdepth}{3}
\setcounter{secnumdepth}{3}
%https://tex.stackexchange.com/questions/17877/how-to-show-subsubsections-and-paragraphs-in-toc/17879#17879

% hyperref must be the last called package (exception geometry package)
\usepackage[pdfborder={0 0 0}, colorlinks=true, linkcolor=blue]{hyperref}
